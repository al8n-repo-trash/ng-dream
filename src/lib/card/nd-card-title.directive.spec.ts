import { NdCardTitleDirective } from './nd-card-title.directive';

describe('NdCardTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new NdCardTitleDirective();
    expect(directive).toBeTruthy();
  });
});
