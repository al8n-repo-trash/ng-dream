import { NdCardAvatarComponent } from './nd-card-avatar.directive';

describe('NdCardAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new NdCardAvatarComponent();
    expect(directive).toBeTruthy();
  });
});
