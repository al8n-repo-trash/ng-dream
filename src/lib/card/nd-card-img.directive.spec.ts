import { NdCardImgDirective } from './nd-card-img.directive';

describe('NdCardImgDirective', () => {
  it('should create an instance', () => {
    const directive = new NdCardImgDirective();
    expect(directive).toBeTruthy();
  });
});
