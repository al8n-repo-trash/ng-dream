import { NdCardContentDirective } from './nd-card-content.directive';

describe('NdCardContentDirective', () => {
  it('should create an instance', () => {
    const directive = new NdCardContentDirective();
    expect(directive).toBeTruthy();
  });
});
