import { NdCardSubtitleDirective } from './nd-card-subtitle.directive';

describe('NdCardSubtitleDirective', () => {
  it('should create an instance', () => {
    const directive = new NdCardSubtitleDirective();
    expect(directive).toBeTruthy();
  });
});
