import { NdAlignDirective } from './nd-align.directive';

describe('NdAlignDirective', () => {
  it('should create an instance', () => {
    const directive = new NdAlignDirective();
    expect(directive).toBeTruthy();
  });
});
