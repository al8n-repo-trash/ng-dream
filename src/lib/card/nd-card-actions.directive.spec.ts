import { NdCardActionsDirective } from './nd-card-actions.directive';

describe('NdCardActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new NdCardActionsDirective();
    expect(directive).toBeTruthy();
  });
});
